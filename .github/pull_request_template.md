## Summary

<!-- What does this change, and why? -->

## Checklist

- [ ] Tests pass (`uv run --project reference pytest reference -q`)
- [ ] Lint and types pass (`uv run --project reference ruff format --check reference bench`, `ruff check reference bench`, `basedpyright`)
- [ ] Updated the README if behaviour, arguments or accuracy changed
- [ ] Checked NOTICE if a dependency or its licence moved
